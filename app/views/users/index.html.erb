<nav class="bg-gray-100">
  <div class="max-w-5xl mx-auto">
    <div class="flex justify-between">
      <div class="flex space-x-4">
        <div>
          <a href="#" class="flex items-center py-5 px-2 text-gray-700">
            <svg xmlns="http://www.w3.org/2000/svg"
              class="h-6 w-6 mr-1 text-blue-400" fill="none"
              viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round"
                stroke-width="2"
                d="M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0
                001.555.832l3.197-2.132a1 1 0 000-1.664z" />
              <path stroke-linecap="round" stroke-linejoin="round"
                stroke-width="2" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <span class="font-bold">myTunes</span>
          </a>
        </div>
        <div class="flex items-center space-x-1">
          <div class="py-5 px-2 text-gray-700">
            <%= link_to 'Friends', friendship_path(id: current_user.id) %>
          </div>
          <div class="py-5 px-2 text-gray-700">
            <%= link_to 'Feed', posts_path %>
          </div>
          <div class="py-5 px-2 text-gray-700">
            <%= link_to 'New Post', new_post_path %>
          </div>
          <div class="py-5 px-2 text-gray-700">
            <%= link_to 'Edit Profile', edit_user_path(current_user.id) %>
          </div>
          <div class="py-5 px-2 text-gray-700">
            <%= link_to 'View Profile', user_path(current_user.id) %>
          </div>
          <svg xmlns="http://www.w3.org/2000/svg"
            class="h-6 w-6 mr-1 text-blue-400" fill="none"
            viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round"
              stroke-width="2"
              d="M8.228 9c.549-1.165 2.03-2 3.772-2 2.21 0 4 1.343 4 3 0
              1.4-1.278 2.575-3.006 2.907-.542.104-.994.54-.994 1.093m0
              3h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          <% if current_user.pending_friend_requests.empty? %>
            No Pending Friend Requests
          <% else %>
            <%= link_to 'Pending Friend Requests', friendships_path %>
          <% end %>
        </div>
      </div>
      <div class="flex items-center space-x-1">
        <a href="" class="py-5 px-3">Log in</a>
        <a href="" class="py-3 px-3 bg-yellow-400 text-yellow-900 rounded">Sign up</a>
      </div>
    </div>
  </div>
</nav>

<div class="py-8">
  <p class="font-extrabold text-6xl">
    Logged in as <%= current_user.name %><br><br>
    All Users<br>
    <% @users.each do |u| %>
      <% unless u == current_user %>
        <%= link_to u.name, user_path(u.id) %> -
        <%= u.project_name %>
        <% if current_user.can_friend_request? u %>
          <%= link_to '- Make Friend Request',
            friendships_path(
              friendship: { requesting_user_id: current_user.id,
                            requested_user_id: u.id, confirmed: false }),
              method: :post %>
        <% end %><br>
      <% end %>
    <% end %><br>
    <%= link_to 'Sign out', destroy_user_session_path, method: :delete %>
  </p>
</div>
