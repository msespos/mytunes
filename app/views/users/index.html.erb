<h1>Users</h1>
<% @users.each do |u| %>
  <%= u.name %> -
  <%= u.project_name %> -
  <%= link_to 'Request Friend', root_path %><br>
<% end %>
<%= link_to 'Sign out', destroy_user_session_path, method: :delete %>
