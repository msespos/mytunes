<nav class="bg-yellow-300">
  <div class="max-w-5xl mx-auto">
    <div class="flex justify-between">
      <div class="flex">
          <div class="flex items-center ml-0 mr-3 md:ml-6 lg:ml-0 py-5 px-0 sm:pl-6 sm:pr-2
                      md:pl-0 text-gray-700">
            <span class="hidden sm:block">
              <%= link_to image_tag('logo.jpg', class: "ml-0 w:2 md:w-24 lg:w-36
                  rounded-lg border-4 border-purple-900"), posts_path %>
            </span>
            <span class="sm:hidden">
              <%= link_to image_tag('logo_mini.jpg', class: "ml-4 w-16
                  rounded-lg border-4 border-purple-900"), posts_path %>
            </span>
          </div>
        <div class="flex items-center space-x-1">
          <div class="py-5 pl-2 md:pl-2 text-gray-700 hover:text-gray-500
                      text-xs md:text-base lg:text-xl font-bold">
            <%= link_to 'Everybody', users_index_path %>
          </div>
          <% unless current_user.pending_friend_requests_from_others.empty? %>
            <div class="rounded-full mb-5 mr-8 px-1 bg-red-500 hover:bg-red-400 text-white
                        text-xs border-white border-2">
              <%= link_to current_user.pending_friend_requests_from_others.size,
                  users_index_path %>
            </div>
          <% end %>
          <div class="py-5 pl-2 lg:pl-4 text-gray-700 hover:text-gray-500 text-xs
                      md:text-base lg:text-xl font-bold">
            <%= link_to 'New Post', new_text_post_path %>
          </div>
        </div>
      </div>
      <div class="flex items-center space-x-1 mr-4">
        <div class="py-5 pl-4 px-2 md:px-4 md:mr-2 text-gray-700 hover:text-gray-500 text-xs
                    md:text-base lg:text-xl font-bold">
          <%= link_to current_user.name, user_path(current_user.id) %>
        </div>
        <div class="py-2 px-3 w-16 md:w-auto bg-yellow-500 hover:bg-yellow-200
                    text-gray-800 text-xs md:text-base lg:text-xl font-bold
                    hover:text-gray-600 border-4 border-purple-900 rounded">
          <%= link_to 'Sign out', destroy_user_session_path, method: :delete %>
        </div>
      </div>
    </div>
  </div>
</nav>
