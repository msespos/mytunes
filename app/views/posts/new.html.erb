<%= form_with model: @post do |f| %>
  <p>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.label :post %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>